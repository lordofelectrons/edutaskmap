{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "installalldeps": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run server --prefix backend\" \"npm start --prefix frontend\""
  }
}
